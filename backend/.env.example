DATABASE_URL=postgresql://tcp:tcp_dev_only@localhost:5432/tcp
CORS_ORIGINS=http://localhost:5173,http://localhost:8080
PORT=8000

# 本地 UAT 演示账号种子开关：留空（默认）不创建任何演示账号。
# 启用时在 shell/compose 环境显式提供强密码，仓库不提供任何默认值；
# 密码仅用于启动时哈希写入，不会被记录或返回。请像对待真实密码一样轮换。
DEMO_SEED_PASSWORD=
