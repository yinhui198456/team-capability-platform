# Migration Instructions

- 新迁移使用下一个连续的 `vNNNN_<name>.py` 版本，并在 `runner.py` 顺序注册；开始前确认当前版本和目标 schema 与 `docs/03_Data.md` 一致。
- 先有证明旧行为缺口的红测。授权测试环境中验证升级链、全新库链、重复执行安全及非目标表/数据保持；按风险运行针对性和受影响检查。
- 不自动回滚、破坏性修复或直接写共享数据库；不重置、恢复、重播或清理数据。
- schema、版本历史、数据保留或测试环境身份任一不确定时停止并请求澄清。
