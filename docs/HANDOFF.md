# TCP 当前交接

更新日期：2026-08-03。

## 当前进度

- `Issue #63 学习执行与 Evidence 闭环：第三阶段工程收口` 已完成：新增跨角色真实 API E2E `frontend/tests/e2e/smoke/issue-63-execution-evidence.spec.ts`（E2E-63-01..05：Member 执行链 / Buddy 评审闭环 / 401·403 权限边界 / 409 冲突恢复与幂等 / 三视口），并修复一处生产缺陷（双角色 Buddy 账号经 Member 路径查询评审历史被 403，`get_task_evidence_reviews` 现回退 Buddy 路径，附 2 条后端回归测试，红→绿）。受影响 shard（issue-62 buddy-review 10/10、后端相关套件 47/47）通过；文档（02_Design / 03_Data / 04_UI / 05_Development / acceptance）已对齐真实路由、六态、结论白名单与 CAS/幂等语义。验收记录：`docs/acceptance/ISSUE_63_ENGINEERING_CLOSEOUT.md`。PR #71 保持 Draft；#63 UAT 未执行、不得视为通过；不进入 #64/#65。
- Git Project 已补齐既有 UAT 卡的可执行 checklist；用户 UAT 仍待执行。
- `6C-1：Codex 浏览器 UI/UX 验收（UI-01～UI-05）` 记录了修复前的原型差异；其结论已由后续 6A-0、6A-5～6A-8、6B-6 修复和 6C-2 真实容器复验覆盖。
- `6A-0：共享导航、角色菜单与视觉框架` 已完成：深蓝顶部模块导航、浅蓝工作区、侧边菜单、按有效角色过滤的入口，以及“本人/负责成员/团队/全量”数据范围提示均已落地；未修改业务数据或后端鉴权。
- `6A-5：UI-01 Member 看板` 已完成：年度进度环、四项学习时长、待办、六域 SVG 雷达、能力域筛选/GAP 表，以及任务名称、L3、计划月份和状态表均已落地，并已纳入 6C-2 真实容器复验。
- `6A-6：UI-04 Buddy 复核中心` 已完成本地实现与隔离浏览器复测：辅导成员筛选、三类复核队列、只读工作区与反馈历史均已验证；Buddy 读取 Evidence 历史时只允许访问负责成员。
- `6A-7：UI-02 自评与 Gap 分析` 已完成：年度/版本/Review/计划门禁摘要、可编辑 L3 表、Gap 与优先级、Gap 统计侧栏和正式计划门禁均已落地，并已纳入 6C-2 真实容器复验。
- `6A-8：UI-03 年度计划、任务与 Evidence` 已完成：计划总览、状态/时长、1–12 月时间轴、计划项详情联动、任务执行摘要、日志、Evidence 版本与 Review 历史均已落地，并已纳入 6C-2 真实容器复验。
- `6B-6：UI-05 Leader 分析` 已完成本地实现与隔离浏览器复测：年度/成员/能力域筛选、四项 KPI、实际-vs-计划、成员热力表、计划和时长组合图、延期明细均已验证；浏览器请求均为 GET，未写入 UAT 数据。前端 `19 files / 119 tests`、build、lint 与 `git diff --check` 通过。
- `6C-2：真实容器浏览器 UI/UX 复验` 已完成并通过：当前工作树经 `docker compose up -d --build` 重建，后端 `/ready` 健康；Member、Buddy、Leader、Admin 共 11 个业务页面的已登录截图通过，Member/Buddy/Leader 的只读交互与 Member→Leader 越权反馈通过，`bash scripts/e2e-smoke.sh` 通过。
- 本轮后端全量回归已在固定工作簿 Docker 挂载下通过：`230 passed`；测试模块已显式封装其跨模块夹具，避免收集顺序影响全量回归。Issue #63 在其后新增 2 条评审历史回归测试（`test_evidence_review.py` 16/16）。

## 浏览器验收结果

| 案例 | 结论 |
|---|---|
| 四角色登录、对应导航 | 通过 |
| Member 自评、Gap、年度计划、任务、档案导航 | 通过 |
| Admin 页面只读呈现 | 通过 |
| 受限页面无权限反馈 | 通过 |
| 修复前基线：UI-01 原型对照 | 历史不通过：缺年度计划进度、四项学习时长、六域雷达；已由后续修复和 6C-2 覆盖。 |
| 修复前基线：UI-04 原型对照 | 历史不通过：缺成员选择、复核队列、工作区、历史与反馈区；已由后续修复和 6C-2 覆盖。 |
| 修复前基线：UI-05 原型对照 | 历史不通过：图表化视觉基线不足；已由后续修复和 6C-2 覆盖。 |
| 6A-0 共享壳层 | 通过：单测覆盖 Member 菜单与范围；隔离浏览器实例截图确认顶部、侧边与公共范围布局。完整已登录浏览器复测待容器重新构建后执行。 |
| UI-01 修复后隔离浏览器复测 | 通过：四项时长、进度环、待办、SVG 雷达、Gap 筛选与任务计划月份可呈现；使用拦截的只读 Member 响应，未写入 UAT 数据。后续已纳入 6C-2 真实容器复验。 |
| UI-04 修复后隔离浏览器复测 | 通过：选择成员后队列联合筛选；自评显示当前/目标/Gap 与只读历史；Evidence 显示版本内容与只读反馈历史。使用拦截的只读 Buddy 响应，未写入 UAT 数据。后续已纳入 6C-2 真实容器复验。 |
| UI-02 修复后隔离浏览器复测 | 通过：年度/版本/Review/门禁摘要、L3 自评表及其编辑控件、Gap 明细、优先级、候选项和统计侧栏均已验证；未触发保存或提交，未写入 UAT 数据。后续已纳入 6C-2 真实容器复验。 |
| UI-03 修复后隔离浏览器复测 | 通过：计划页的月度筛选、计划项详情联动，任务页的执行日志、Evidence 版本、Review 历史和摘要均已验证；使用拦截的只读响应，未写入 UAT 数据。后续已纳入 6C-2 真实容器复验。 |
| UI-05 修复后隔离浏览器复测 | 通过：Leader 的筛选联动、四项 KPI、实际-vs-计划、热力表、两张组合图和延期明细均已验证；所有 API 请求均为 GET。后续已纳入 6C-2 真实容器复验。 |
| 6C-2 真实容器复验 | 通过：重建后使用演示角色实际登录，Member（看板、自评/历史、Gap、年度计划、任务、档案）、Buddy（审核中心、Evidence Review）、Leader（团队能力分析）、Admin（系统管理）共 11 页均可达并已截图；筛选/成员/队列交互仅发 GET，Member 访问 Leader 分析被拒绝。 |

## 未解决问题与风险

- UI-01～UI-05 的 Codex 真实容器浏览器复验已通过；该结论基于关键区域与交互截图，不使用像素级自动比对。
- 角色菜单已按有效角色过滤；后端受限页反馈仍作为直接 URL 访问的兜底，不替代后端授权。
- 用户数据写入型 UAT 尚未执行；待执行卡为 4-5、5-4、6A-4、6B-5、7-4，以及 Issue #63 UAT（`docs/acceptance/ISSUE_63_ENGINEERING_CLOSEOUT.md` §5）。
- 本轮真实容器复验只执行登录、读取、筛选和导航；未执行任何业务数据写入。用户 UAT 与发布决策仍未执行。

## 下一步

1. 由用户执行对应 UAT；其写入型业务操作与本轮 Codex 只读浏览器复验不同。
2. UAT 结论确认后，执行 7-4 的最终回归与发布决策。

## 验证入口

- 浏览器行为 smoke：`TCP_UIUX_PASSWORD=<approved-test-password> python3 scripts/uiux-smoke.py`
- 既有 API/容器 smoke：`bash scripts/e2e-smoke.sh`
- 重启回归：`TCP_E2E_RESTART=1 bash scripts/e2e-smoke.sh`
