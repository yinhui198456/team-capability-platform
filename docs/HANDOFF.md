# TCP 交接

## Issue #194 范围

- Member 首批闭环：M01 我的工作台、M02 能力评级与提升计划、M03 年度成长计划、M04 学习任务、M05 学习任务详情。
- Buddy 仅保留 B01 Evidence Review；评估历史、月度复盘和成长档案保留直达路由，不进入首批 Member 导航。
- M02 保留评级、优先级、加入/移出提升计划、YYYY-MM 计划月份、草稿自动保存，以及“保存能力评级”和“生成所选学习任务”两个独立主操作。
- M03 保留唯一“进入任务”语义、月度时间轴和任务执行链。

## 可追溯技术债

- v0016 的历史 Git 来源、关联 Issue 和进入 base/master 的路径仍需端到端可追溯。该债务只涉及可追溯性，不否定已验证的 trigger guard，也不恢复 v0009 过严复合 FK。

## 已完成验证

- M02/M03 已完成权威原型 1440、1024、768 三视口复审；M02 的已加入计划、优先级/月编辑区、四区评级和底部操作，以及 M03 的摘要、时间轴、月卡和唯一入口均符合范围。
- 相关组件回归 156/156 通过；UI-02/UI-03 Chromium 三视口回归 15/15 通过，六张快照已重生。
- Issue #61 M02 四区能力项 1280、1440、1920 三视口回归 3/3 通过，快照已重生。

## 剩余门禁

1. 在明确授权路径上完成 commit/push。
2. 对新 head 运行 CI，并在明确授权后执行 exact-SHA 隔离部署。
3. 在 `tcp-issue-194-uat-isolated` 执行真实 Chrome Member 写入链与三视口复验，然后请求用户 UAT；用户 UAT 与发布决策分别记录，任何一层都不替代另一层。

## 长期禁止项

- 禁止生产和共享数据库操作；application exact-SHA 部署仅在明确授权门禁下进行；不自动 Ready、merge、close 或发布。
- 不 force-push，不触碰其它 Issue 的分支、PR 或证据。
- 不以截图或单元测试替代原型视觉验收；仅在 Sol 原型复审通过后由 writer 重生 PNG 快照。
