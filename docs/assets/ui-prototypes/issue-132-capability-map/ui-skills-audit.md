# Issue #132 · UI 专业 Skills 审查

## 设计方向

- **视觉主张：** 保留当前 TCP 克制、浅色、企业工具式能力地图；唯一强化点是用同一条蓝色选择语言贯穿 L1 Tab、L2 分组和 L3 目标，让页面选择与地址栏状态形成连续路径。
- **内容计划：** 紧凑页头与搜索 → 六域导航 → 当前域概述 → L2 能力标准 → P4–P8 摘要/完整要求 → L3 达成路径与详情。
- **交互主张：** 用户选择才写入 Hash；浏览器历史负责恢复；搜索组合框提供完整键盘路径；状态切换只使用短促的原生反馈并尊重 reduced motion。
- **视觉来源：** 直接复用仓库当前能力地图 1440×900 / 1920×1080 视觉基线和 `docs/04_UI.md`，因此没有另造三套风格候选，也没有生成装饰图片。

## Skills 逐项审查

| Skill | 采用的专业约束 | 本原型证据 | 结论 |
|---|---|---|---|
| Product Design | 有视觉目标再构建；复用现有产品系统；核心交互必须工作；截图不能替代浏览器比较 | 当前 capability-map baseline 是源视觉；HTML 可操作；`qa/compare-*.png` 为同视口并排比较 | 通过 |
| frontend-skill | App UI 以工作区为主；密集但可读；单一强调色；卡片只在交互本身需要时使用 | 没有营销 Hero、渐变或卡片拼贴；L2/L3 边界只用于展开和选择；蓝色是唯一强调色 | 通过 |
| frontend-design | 设计选择应与主题相关；一处签名元素；构建前后自我批评；响应式、焦点、reduced motion | “蓝色能力路径”是唯一签名；保留 TCP 字体与间距；已执行两轮截图批评和修正 | 通过 |
| frontend-dev | 纯 HTML 是正式输出模式；真实文案；完整交互状态；不使用远程占位资源；无意义依赖不引入 | `index.html` + 本地 CSS/JS；包含搜索空态、无效深链、空 L2、选中/聚焦/Drawer；零依赖、零远程媒体 | 通过 |
| ui-ux-pro-max | URL 反映当前状态；pushState 保留后退；键盘和可见焦点；无横向滚动；语义控件与可恢复错误 | L1/L2/L3 Hash、刷新/后退/前进、Arrow/Home/End/Enter/Escape、3px 焦点环、无效路径恢复、四视口检查 | 通过 |

## 审查中发现并修正

1. **P2 · 原型曾在页面内重复显示“当前路径”。** 地址栏已承担该职责，额外标签偏离现有视觉基线；已删除，避免提示文案泄漏进产品界面。
2. **P2 · 默认 P01/P02 列表最初只有部分 L2。** 虽足够演示，但与正式信息密度不一致；已补齐 P01 十组和 P02 十组，其中 P02.07–P02.10 保留明确空路径语义。
3. **P3 · 全页截图曾出现跳转链接拼接伪影。** 将 skip link 的隐藏方式改为负 `top`，键盘聚焦时仍可见，浏览器证据中不再产生伪影。
4. **移动操作密度。** 搜索、清除、工具按钮和 Drawer 关闭按钮在窄视口统一达到至少 44px；桌面 Web 仍保持紧凑。

## 最终检查

- 无 P0/P1/P2 未解决项。
- 未使用 Emoji、CSS 图形、手绘 SVG、远程图片、占位图片或新依赖。
- 未实现 API、认证、持久化、Leader 编辑或其他路由；它们不是 #132 阶段 1 的交互目标。
- 示例数量和文案只服务于评审，不反向定义生产数据。
